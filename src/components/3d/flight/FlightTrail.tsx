


export default function FlightTrail() {
  // This component is now a placeholder for future 3D flight trail rendering only.
  // Info overlays are handled in Flights.tsx to avoid double popups.
  return null;
}
